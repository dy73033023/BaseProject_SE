# 게임 개발 지침

이 문서는 현재까지 사용자와 합의한 프로젝트 방향과 Codex 작업 기준이다. 이후 작업에서도 기본 지침으로 사용하며, 사용자의 새로운 지시가 있으면 해당 내용을 반영한다.

## 개발 환경과 기본 도구

- Codex를 기반으로 현재 프로젝트에서 개발을 진행한다.
- 기존 C++ / OpenGL / GLEW / FreeGLUT 및 Visual Studio 솔루션을 기본 개발 기반으로 유지한다. 별도 요청 없이 다른 게임 엔진이나 도구 체계로 전환하지 않는다.
- 작업 공간: `C:\GameSoftwareEngineering`
- 솔루션: `BaseProject_SE/SimpleGame.sln`
- 소스 폴더: `BaseProject_SE/SimpleGame`
- 현재 프로젝트 설정은 MSVC v143 및 Windows SDK 10.0을 사용한다.

## 확정된 게임 목표

- 실시간 렌더링 기반의 오픈월드 RPG를 개발한다.
- 시점은 쿼터뷰이며 캐릭터 움직임은 2.5D로 표현한다.
- 시간적 배경은 《워해머 40,000: 스페이스 마린 2》와 같은 배경으로 한다.
- 분위기는 해당 작품과 같은 암울한 디스토피아로 한다.
- 워해머 40,000 세계관과 타이투스를 그대로 사용한다. 독자적인 대체 주인공을 만드는 방향이 아니다.
- 기존 작품의 사건을 재현하되 세계관을 모르는 플레이어도 점차 이해할 수 있도록 전달 방식을 다듬는다.

## 스토리 전달 기준

- 기존 작품의 주요 사건과 타이투스의 행적을 바탕으로 한다.
- 이해를 돕는 설명과 연출을 추가하되, 원작 사건의 원인·결과나 인물의 동기를 임의로 변경하지 않는다.
- 각 임무에서 어디로 가는지, 무엇을 해야 하는지, 왜 중요한지를 명확히 전달한다.
- 인물, 세력, 전문 용어는 필요한 시점에 짧고 쉬운 설명으로 소개한다.
- 과거 사건은 현재 상황과 인물 관계를 이해하는 데 필요한 만큼 대화나 회상으로 보충한다.
- 방대한 세계관 지식을 시작부터 요구하지 않는다. 상세 설정은 선택적으로 확인할 수 있는 기록 등의 형태를 고려한다.
- 구체적인 원작 사건을 구현할 때는 자료를 확인하고, 확인된 설정과 창작 보충 부분을 구분한다.
- 이전 대화의 '연락이 끊긴 도시에서 생존자를 찾는 임무'는 창작 예시일 뿐, 확정된 도입부나 원작 사건으로 취급하지 않는다.

## 미정 사항

- 현재 튜토리얼은 2편 캠페인 초반을 재현한다. 이후 전체 게임의 재현 범위는 미정이다.
- 구체적인 오픈월드 규모, 전투 방식, 퀘스트 구성, 캐릭터 그래픽 제작 방식은 아직 확정되지 않았다.

## 구현 시 고려할 방향

다음은 목표를 지원하는 설계 방향이며, 이미 구현된 기능이나 확정된 세부 사양으로 취급하지 않는다.

- 입력 처리 → 시간 기반 게임 상태 업데이트 → 렌더링 흐름을 구성한다.
- 게임 로직과 렌더링의 역할을 분리한다.
- 월드 좌표와 화면 좌표를 분리하고 쿼터뷰 투영을 적용한다.
- 바닥 평면 이동과 높이 값을 분리하여 2.5D 움직임을 표현한다.
- 캐릭터와 배경 오브젝트의 위치에 따라 앞뒤 겹침을 처리한다.
- 카메라 추적, 지형·장애물 충돌을 고려한다.
- 월드 확장 시 청크 단위 로딩과 화면 밖 렌더링 제외를 고려한다.
- 주요 스토리 순서와 자유 탐험을 함께 지원하는 구성을 고려한다.
- 작은 필드의 이동·카메라·충돌·깊이 정렬 기반부터 점진적으로 확장한다. 처음부터 모든 RPG 시스템을 구현하지 않는다.

## Codex 작업 방식

- 특별한 상황이 아니면 빌드와 실행 결과 확인은 사용자가 담당한다.
- 기본적으로 코드 분석, 수정 및 정적 검토까지만 수행한다. 일반적인 검증을 이유로 자동으로 빌드하거나 게임을 실행하지 않는다.
- 직접 빌드 또는 실행 확인이 꼭 필요한 특별한 상황이면 그 이유를 먼저 설명한다.
- 결과 보고에서 실제로 수행한 검토와 수행하지 않은 빌드·실행을 명확히 구분한다.
- 요청 범위에 맞게 작업하며, 기획 논의만으로 미정 사항을 확정하거나 관련 없는 기능을 구현하지 않는다.
- 사용자와의 설명 및 프로젝트 문서는 기본적으로 한국어를 사용한다.

## 초기 구현 상태 참고

시각 개선 요구(2026-09-09): 후처리, 그림자, 캐릭터 스프라이트 애니메이션, 지형·길·환경 재질과 이펙트를 개선하고 사용자 화면을 한국어로 통일한다. 기존 `Renderer.cpp` 및 프로젝트를 갱신하는 방식을 유지하며 필요하면 소스 파일과 자산을 추가한다. 원작 튜토리얼과의 일치도를 높이되 기존 기획서를 유지·갱신하고, 축약·미구현 범위를 기록한다. 실제 빌드·실행 검증은 사용자가 맡는다.

추가 요구(2026-09-09): 약 5분 분량의 튜토리얼 맵, WASD 이동, 적절한 상호작용 키, 쿼터뷰 2.5D와 디스토피아 분위기의 렌더링, 2편 캠페인 초반 재현, 짧은 목표 안내 텍스트를 구현한다. 현재 구현 범위와 조작법은 `튜토리얼_프로토타입.md`를 참고한다. 전투 수치와 방어 시간은 초기 조정값이며 실제 플레이 검증 후 조정한다.

2026-09-07 소스 분석 기준:

- 창 생성, 셰이더 로딩, 정점 버퍼 생성, 단색 사각형 렌더링이 구현되어 있다.
- 입력 콜백은 등록되어 있으나 게임 이동이나 상태 변경은 구현되어 있지 않다.
- 오픈월드, 캐릭터 애니메이션, 충돌, 전투, 퀘스트, 저장 시스템은 아직 구현되어 있지 않다.
- 이 항목은 초기 상태 기록이다. 이후 실제 소스와 변경 내용을 우선한다.
